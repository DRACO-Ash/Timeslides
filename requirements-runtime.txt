# Runtime dependencies only. This is what the container image installs, so it
# is what the container scan judges. Keep test tooling out of it.
astropy==8.0.1        # GCRS to TEME conversion; UDL state vectors are J2000
fastapi==0.141.1
numpy==2.4.6
plotly==7.0.0         # inlined into the report, never fetched from a CDN
pydantic==2.13.5      # boundary validation on every request body
requests==2.34.2
sgp4==2.27
uvicorn==0.52.4
